Project Name: Test Yempo Web API that consume in Console application:
Project Description: Application was developed in .NET Core(6) using Web API that consume within Console Application.


Instruction how to run the application:
1. Pls change the Connection Strings in appsettings.json if necessary.
2. From "Package ManagerConsole" run Update-Database
   under Tools MENU, Nugget Package Manager, Package Manager Consule
   seeded records are included after running the update-database.
3. Run the application with Multiple Startup. 